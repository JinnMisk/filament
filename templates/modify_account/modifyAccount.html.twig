{% extends 'base.html.twig' %}

{% block title %}Modifier mon compte
{% endblock %}

{% block body %}

	{{ form_start(userForm) }}

	{# <div class="form-group">
		{{ form_row(modifyAccount.email, {'label': 'Email', 'attr': {'placeholder': 'Entrez votre adresse mail'}}) }}
	</div>
	<div class="form-group">
		{{ form_row(modifyAccount.first_name, {'label': 'Prénom', 'attr': {'placeholder': 'Entrez votre prénom'}}) }}
	</div> #}
	<button type="submit" name="modify_account_submit" class="btn btn-warning btn-block">Enregistrer</button>

	{{ form_end(userForm) }}

{% endblock %}
