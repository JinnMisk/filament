{% block body %}
<!-- FORMULAIRE DE MODIFICATION D'UNE PROGRAMMATION -->
<div class="modal fade" id="reg-modal" tabindex="-1" aria-labelledby="modal-title" aria-hidden="true">
    <div class="modal-dialog modal-dialog-scrollable modal-dialog-centered modal-lg">
        <div class="modal-content">

            <!-- TITRE -->
            <div class="modal-header">
                <h5 class="modal-title" id="modal-title">Modifier ma programmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            
            <!-- FORMULAIRE -->
            <div class="modal-body">
                {% form_theme modalModifyScheduleForm 'form/modify_schedule_form.html.twig' %}
                {{ form_start(modalModifyScheduleForm) }}

                <div>
                    <div class="row">
                        <div class="col-6 d-flex justify-content-center">
                            {{ form_row(modalModifyScheduleForm.label, {'label': 'Nom du programme', 'attr': {'placeholder': ''}}) }}
                        </div>
                            
                        <div class="col-6 d-flex justify-content-center">
                          {{ form_row(modalModifyScheduleForm.mood_id, {'label': 'Ambiance', 'attr': {'placeholder': ''}}) }}
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-6 d-flex justify-content-center">
                          {{ form_row(modalModifyScheduleForm.start_hour, {'label': 'Heure début', 'attr': {'placeholder': ''}}) }}
                        </div> 
                        
                        <div class="col-6 d-flex justify-content-center">
                            {{ form_row(modalModifyScheduleForm.start_day, {'label': 'Jour début', 'attr': {'placeholder': ''}}) }}
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-6 d-flex justify-content-center">
                          {{ form_row(modalModifyScheduleForm.end_hour, {'label': 'Heure fin', 'attr': {'placeholder': ''}}) }}
                        </div>
                        
                        <div class="col-6 d-flex justify-content-center">
                          {{ form_row(modalModifyScheduleForm.end_day, {'label': 'Jour fin', 'attr': {'placeholder': ''}}) }}
                        </div>
                    </div>
                  
                    <div class="row">
                        <div class="col-6 d-flex justify-content-center">
                          {{ form_row(modalModifyScheduleForm.recurrence, {'label': 'Récurrence', 'attr': {'placeholder': ''}}) }}
                        </div>
                    </div>
                
                </div>

                <!-- BOUTONS -->
                <div class="row">
                    <div class="modal-footer col-12 d-flex justify-content-center">
                      <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
                      <button type="submit" name="add_bulb_submit" class="btn btn-warning btn-block">Enregistrer</button>
                    </div>
                </div>

                {{ form_end(modalModifyScheduleForm) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}